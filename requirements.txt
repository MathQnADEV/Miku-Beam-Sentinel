# Core scanning engine
requests>=2.31.0
beautifulsoup4>=4.12.0
python-dotenv>=1.0.0
colorama>=0.4.6

# Web backend (Django REST + WebSockets via Channels/Daphne)
django>=5.0.0
djangorestframework>=3.14.0
django-cors-headers>=4.3.0
channels>=4.0.0
daphne>=4.0.0

# Optional: only needed if you set DB_ENGINE=mysql in .env.
# Requires system MySQL/MariaDB client headers to build.
# mysqlclient>=2.2.0
